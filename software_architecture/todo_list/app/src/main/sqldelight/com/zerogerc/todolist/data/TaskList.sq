CREATE TABLE task_list (
    _id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL
);

selectAll:
SELECT *
FROM task_list;

addTaskList:
INSERT INTO task_list(title)
VALUES (?);

deleteTaskList:
DELETE FROM task_list
WHERE _id = ?;
